services:
  stream-watchdog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stream-watchdog
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3030:3030"
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
